[package]
name = "tactical-locale"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
# Zero external dependencies — the HOI4-style localisation parser is hand-rolled.
